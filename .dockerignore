# 让 docker build 只发必要文件,避免把 node_modules(300MB+)/媒体/git 历史塞进构建上下文。
# Dockerfile 实际 COPY 的:console/(前端,会 npm ci 重建)、pyproject.toml、src/、config/、scripts/、eval/。
.git/
.gitignore
.claude/
.vscode/
**/node_modules/
.venv/
venv/
console/dist/
console/tsconfig.tsbuildinfo
build/
dist/
*.egg-info/
UNKNOWN.egg-info/
__pycache__/
.pytest_cache/
data/
logs/
secrets/
infra/
docs/
# 仅排除「仓库根」的游离大文件(镜像内用的是 src/sales_agent/integrations/dingtalk/static/ 下的正式副本)。
# 必须用前导 / 锚定根目录——裸名(如 cocah.html)会被 BuildKit 递归匹配,误删
# src/.../static/cocah.html + coach_mode.png,导致 /integrations/dingtalk/quick 返回
# 500 "H5 template not found"(见 changelog 2026-06-25、tasks/lessons.md §11)。
/coach_mode.png
/cocah.mp4
/cocah.html
=0.20.0
*.md
Dockerfile
docker-compose*.yml
