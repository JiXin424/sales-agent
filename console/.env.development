# vite dev 模式：浏览器走同源 /api/*，由 vite 代理转发到后端（见 vite.config.ts 的 server.proxy）。
# 用相对前缀而非 http://localhost:<port>，确保浏览器与服务器不同机时也能工作。
# 后端实际端口在 vite.config.ts 的 proxy.target 维护（本机 8003，与 deploy/tenants.json 一致）。
# 生产构建为同源（见 .env.production，此处留空）。
VITE_API_BASE_URL=/api
