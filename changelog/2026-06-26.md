# 2026-06-26 升级日志

## 清理本机（开发机）闲置的 Gitea + act_runner

### 概述
本机（开发机 `172.25.186.209`）的 `infra/cicd-compose.yml` 里有一套主控 `47.120.55.219`
CI 的**闲置副本**（gitea 服务 + systemd `gitea-runner.service` + `gitea-data` 卷），配置连 IP
都克隆自主控。本机从不在其上 push（仓库停老 sha `e067a4f`），本机生产容器也从主控 registry
拉镜像（`registry.internal`→`172.25.186.210`），故纯冗余，清除；仅保留 infra 的 registry。

### 改动明细
- **改动对象**：`infra/cicd-compose.yml` + 本机 docker/systemd 运行态
- **类型**：基础设施清理
- **影响范围**：仅本机开发机。**不影响**主控 CI（独立）、**不影响**本机生产容器（镜像来自主控 registry）。
- **改动明细**：`docker stop/rm gitea` + `docker volume rm infra_gitea-data`；`systemctl disable --now gitea-runner`（停 act_runner 防孤儿重连）；`cicd-compose.yml` 移除 gitea 服务 + `gitea-data` 卷声明并更新注释（防 `up -d` 重建），registry 保留。
- **原因**：闲置副本无用途，留着误导（写死主控 IP）+ act_runner 空转重连。
- **未动**：`infra/htpasswd`、`registry-password.txt`、`domain.crt/key`、`registry-ca.crt`（registry 仍 Up，可能在引用）；`gitea-admin-{password,token}.txt` 为本机 gitea 遗留凭证，已无用，未删（可选）。

### 验证
`docker ps`/`volume ls` 无 gitea/gitea-data；`ps aux` 无 act_runner；`gitea-runner.service` inactive+disabled；`compose config --services` 仅 registry；`sales-agent-registry` 仍 Up。

### 教训关联
此闲置副本正是 lessons #18 的起因——曾把它误当成「主控 CI」查了五六轮。清除后从根上避免再被误导。

---

## 知识库（ontology_neo4j）支持上传 .doc 与 .xlsx

### 概述
生产知识库走 Neo4j 本体引擎（`POST /agents/{agent_id}/ontology/ingest`），原白名单仅
`.md/.txt/.docx/.pdf/.pptx`。本次新增 **旧版 `.doc`**（OLE2 二进制，`python-docx` 不支持）与
**`.xlsx`** 两种格式的上传解析，解析为纯文本后进入现有 LLM 实体/事实抽取 → Neo4j 入库链路。

- `.doc`：用 **LibreOffice 无头转换**（`soffice --headless --convert-to docx`）转成 `.docx`，
  再复用 python-docx 段落抽取。选 LibreOffice 而非 antiword/catdoc 的原因：中文 `.doc` 用 antiword
  易乱码、会破坏下游 LLM 抽取质量，LibreOffice 中文支持最好。代价：镜像 `+约 150~200MB`
  （`libreoffice-writer`），单文件转换冷启动慢数秒（300s job 超时兜底）。
- `.xlsx`：用 **openpyxl**（`data_only=True` 取公式缓存值、`read_only=True` 省内存），按 sheet
  遍历行，每 sheet 以 `## <sheet名>` 作小标题、行内单元格 tab 连接，保留表格结构供 LLM 理解。

---

### 改动明细

#### 1. 依赖
- **改动对象**：`pyproject.toml`、`Dockerfile`
- **类型**：依赖新增
- **影响范围**：后端镜像构建
- **改动明细**：
  - `pyproject.toml` dependencies 新增 `openpyxl>=3.1.0`（`.xlsx` 读取；项目原只有 `xlsxwriter` 只能写）。
  - `Dockerfile` apt-get install 列表新增 `libreoffice-writer`（含 `soffice` 命令 + Writer 的 `.doc/.docx` 过滤器，足够无头转换）。
- **原因**：旧版 `.doc` 无可靠纯 Python 解析器；openpyxl 是 `.xlsx` 读取的事实标准。

#### 2. 上传白名单
- **改动对象**：`src/sales_agent/api/routes/ontology.py`
- **类型**：配置
- **影响范围**：`/agents/{id}/ontology/ingest` 上传校验
- **改动明细**：`ALLOWED_EXTENSIONS` 由 `{.md,.txt,.docx,.pdf,.pptx}` 扩为 `{.md,.txt,.docx,.doc,.pdf,.pptx,.xlsx}`（校验逻辑集合驱动，无需改）。
- **原因**：原白名单直接 400 拒绝 `.doc/.xlsx`。

#### 3. 文件解析
- **改动对象**：`src/sales_agent/ontology/ingestion_service.py`
- **类型**：功能增强（+ 重构）
- **影响范围**：`_read_content()` 文本抽取
- **改动明细**：
  - 抽出 `_docx_text(path)` helper，`.docx` 分支复用（行为不变，纯重构）。
  - 新增 `_doc_to_text(src)`：每次调用独立 `TemporaryDirectory` + `UserInstallation` profile（避免并发入库的 profile 锁竞争），`subprocess` list 形参调 soffice（文件名含中文/空格也安全），转 `.docx` 后 `_docx_text` 读文本；soffice 未装时抛 `"未安装 LibreOffice"`。
  - 新增 `.xlsx` 分支：openpyxl 按 sheet 遍历，`## <title>` 小标题 + 行单元格 tab 连接。
  - `_read_content` 新增 `.doc` 分发分支；`.docx`/`.doc`/`.xlsx` 失败统一包装 `RuntimeError("xxx 解析失败：<文件名>")`，与现有 pdf/pptx 风格一致。
- **原因**：让两种新格式产出可供 LLM 抽取的纯文本；`_read_content` 仍同步内联调用（与现有 pdf/pptx 一致，300s 超时兜底）。

#### 4. 前端
- **改动对象**：`console/src/pages/Agents/AgentKnowledgePage.tsx`
- **类型**：UI
- **影响范围**：Agent 知识页上传组件
- **改动明细**：`<Dragger accept>` 加 `.doc,.xlsx`；提示文案更新为「支持 .md / .txt / .doc / .docx / .pdf / .pptx / .xlsx」。
- **原因**：前端文件选择器同步放开新格式。

#### 5. 测试
- **改动对象**：`tests/unit/ontology/test_read_content.py`
- **类型**：测试
- **影响范围**：`_read_content` 单测
- **改动明细**：新增 `.doc`（mock `_doc_to_text`）、`.doc` 失败、`_doc_to_text` 缺 soffice 守卫、`.xlsx`（mock openpyxl，校验 sheet 标题 + tab 连接 + 全空行跳过）、`.xlsx` 失败 共 5 个用例；现有 5 个用例回归。
- **原因**：CI 无需装 LibreOffice/openpyxl 真实跑（全 mock）。

### 验证
- `.venv/bin/pytest tests/unit/ontology/test_read_content.py -v`：**10 passed**（5 旧 + 5 新）。
- `.venv/bin/pytest tests/unit/ontology/ tests/integration/test_ontology_api.py -q`：**46 passed**。
- 真实 `.xlsx` 冒烟（xlsxwriter 造表 → `_read_content`）：中文 + 多 sheet + 公式缓存值 + tab 分隔均正确。
- `.doc` 真实转换需重建镜像（含 `libreoffice-writer`）后上传真实中文 `.doc` 验证（端到端步骤）。

### 已知权衡
- 镜像 `+约 150~200MB`（用户已确认接受）。
- `.doc` 表格内容随 python-docx paragraphs 抽取（与现有 `.docx` 行为一致，不含表格单元格）；日后需 `.doc` 表格可改转 `txt` 过滤器。

---

## 移除遗留演示假数据 tenant_demo_b 及 tenant-b 部署模板

### 概述
移除早期开发期残留的演示租户 `tenant_demo_b`（企业云盘产品，与真实「网票福多多」业务无关）
及其在文档与部署模板里的引用。经核实：该租户在数据库 **43 张含 tenant_id 的表中零数据**、
`tenants` 表未注册、`eval/smoke_test.jsonl` 不依赖、文件已入 git 可恢复，删除零风险。
同时清理从未实际部署的 `tenant-b`（第二租户示例槽位，`secrets/` 下无对应 env）在部署脚手架
与文档中的引用，精简多租户示例。

---

### 改动明细

#### 1. 删除 tenant_demo_b 假数据目录
- **改动对象**：`data/sales-agent/tenants/tenant_demo_b/`（`faq.md` + `product_intro.md`）
- **类型**：数据删除
- **影响范围**：演示知识库语料（不影响任何真实租户）
- **改动明细**：`git rm` 删除两个企业云盘假数据 md（4 个 Q/A）。
- **原因**：早期 demo 残留，与真实业务无关；数据库已确认零数据，保留只会误导。

#### 2. 清理 tenant-b 部署模板与文档引用
- **改动对象**：`README.md` / `deploy/tenants.test.json` / `scripts/deploy-release.sh` / `docs/deployment-roles.md`
- **类型**：配置 / 文档清理
- **影响范围**：部署脚手架与文档
- **改动明细**：
  - `README.md`：删除 `tenant_demo_b` 的「Demo 数据」表行（标题由「两个演示租户」改「演示租户」）、项目结构图中 `tenant_demo_b/documents/` 与 `tenant-b.env.example` 引用、多租户部署 `cp ... tenant-b.env` 示例。
  - `deploy/tenants.test.json`：删除第二租户 `tenant-b` 模板段，仅保留 `taishan`。
  - `scripts/deploy-release.sh`：删除 usage 示例与代码注释中的 `tenant-b.env` 引用。
  - `docs/deployment-roles.md`：删除 `tenant-b` 的单容器/拆分 profile 示例与服务端口表行。
- **原因**：`tenant-b` 是从未实际部署的示例槽位（`secrets/` 无对应 env，生产 inventory 已剔除），精简脚手架并保持文档与代码一致。

#### 3. 刻意保留（避免误删）
- **改动对象**：`tests/unit/test_secret_resolver.py`、`tests/unit/test_risk_checker.py`、`changelog/2026-06-25.md`
- **类型**：N/A
- **影响范围**：测试与历史日志
- **改动明细**：不改动。`tenant_b` 在单元测试中是**跨租户隔离安全测试**的 fixture 字符串（验证「租户 A 查不到租户 B」），非 demo 数据；`changelog/2026-06-25.md` 中的 `tenant-b` 为不可变历史记录。
- **原因**：防止以「清理 demo」之名误删安全测试或篡改历史日志。

### 验证
- `grep tenant_demo_b|demo_b`：零残留。
- `grep tenant-b`：仅剩 `changelog/2026-06-25.md` 历史记录（正确保留）。
- `python3 -m json.tool deploy/tenants.test.json`：合法 JSON。
- `bash -n scripts/deploy-release.sh`：语法 OK。
- `pytest tests/unit/test_risk_checker.py tests/unit/test_secret_resolver.py`：**31 passed**。

---

## 清理顶层 nginx 死配置（Traefik 已接管 SSL/反代）

### 概述
移除早期单机部署遗留的顶层边缘 nginx 配置（`nginx/` 目录）及其配套 certbot 续签脚本
`scripts/init-letsencrypt.sh`。当前生产部署的 HTTPS 终止与反向代理已由**共享 Traefik 实例**
（`/root/code/traefik/`，`certResolver: letsencrypt` 自动签发/续签证书）接管，域名
`qiyelongxia.com.cn` 实际由 Traefik serve——顶层 nginx 无任何 compose 服务引用、不在请求链路上，
属纯死配置。同步清理 `.gitignore` / `.dockerignore` 中指向它的死引用。

**关键区分**：每租户前端容器（`console/Dockerfile`）内置的 nginx（serve React SPA + `/api` 代理）
是**活的**，Traefik 路由终点即此容器，本次**不动**——Traefik 是纯 L7 路由器，无 `root`/`try_files`，
无法 serve 静态目录、做不了 SPA fallback，替代不了它。

---

### 改动明细

#### 1. 删除顶层边缘 nginx 配置
- **改动对象**：`nginx/nginx.conf`、`nginx/templates/sales-agent.conf.template`
- **类型**：配置删除
- **影响范围**：无（死配置，无 compose 服务引用）
- **改动明细**：`git rm -r nginx/`。原配置承担 `qiyelongxia.com.cn` 的 SSL 终止 + HTTP→HTTPS 重定向 + 反代到 `taishan-api:8000`，现全由 Traefik 承担。
- **原因**：被共享 Traefik 取代后遗留，保留会误导部署者以为还需单独维护 nginx/certbot。

#### 2. 删除 certbot 续签脚本
- **改动对象**：`scripts/init-letsencrypt.sh`
- **类型**：脚本删除
- **影响范围**：无（其调用的 `docker compose --profile nginx-setup run certbot` 服务早已不存在于任何 compose）
- **改动明细**：`git rm`。原脚本用 certbot standalone 申请 Let's Encrypt 证书并装 cron 续期；Traefik 自带 `certResolver: letsencrypt` 自动签发/续签，脚本冗余。
- **原因**：同上，配套死代码。

#### 3. 清理忽略规则死引用
- **改动对象**：`.gitignore`、`.dockerignore`
- **类型**：配置清理
- **影响范围**：忽略规则
- **改动明细**：`.gitignore` 删 `nginx/certbot-etc/`、`nginx/certbot-webroot/` 及其注释；`.dockerignore` 删 `nginx/`。
- **原因**：指向已删目录的死规则。

#### 4. 刻意保留（前端容器 nginx）
- **改动对象**：`console/Dockerfile`、`console/nginx.conf.template`、`console/docker-entrypoint.sh`
- **类型**：N/A
- **影响范围**：前端容器
- **改动明细**：不改动。该 nginx 承担 SPA 静态文件 serve + `/api` 代理到后端，是 Traefik 路由的终点，承担实时流量。
- **原因**：区分「死的边缘 nginx」与「活的前端 nginx」，避免以「清理 nginx」之名误删正在服务的组件。

### 验证
- `grep init-letsencrypt|nginx/certbot|nginx/nginx.conf|nginx/templates|nginx-setup`（排除 `console/`、`changelog/`）：**零残留**。
- Traefik `certResolver: letsencrypt` 规则保留（`scripts/render-multitenant-deploy.py:469,497`）。
- 前端容器 nginx 三件套保留（`console/Dockerfile` / `nginx.conf.template` / `docker-entrypoint.sh`）。

---

## 清理 deploy/ inventory 文件命名与死文件

### 概述
消除 `deploy/` 下 inventory 文件「看似重复」的混乱。核实运行时与 git 状态后澄清真相：
- `deploy/tenants.json` 被 `.gitignore:55` 忽略，是**每台机器本地的真实 inventory**（`deploy-release.sh` 默认读它，不进 git），prod2/prod3 各自一份、互不影响，CI 的 `git reset --hard` 不碰它——**不是重复**。
- git 跟踪的 `tenants.{prod2,prod3,test}.json` 是**无人引用的死文件/旧模板**（prod2/prod3 用本地 gitignored `tenants.json`，CI image-deploy 只 render `tenants.hangzhou.json`），内容已过时（如 prod3.json 写 taishan 8001，实际 prod3 跑 taishanyanshi+songbai）。
- `tenants.hangzhou.json` 是唯一活的 image-deploy inventory，但其目标机 47.118.16.235 实际是 **test**（阿里云 cn-test 地域，文档早有标注），命名错误。

本次：删 3 个死文件、`tenants.hangzhou.json` 改名 `tenants.test.json`、修 CI/脚本/文档引用。**不改任何 inventory 内容、不改变任何机器实际运行的容器**（行为等价）。

### 改动明细
- **删除死文件**：`deploy/tenants.prod2.json`、`tenants.prod3.json`、`tenants.test.json`（旧版）。
- **改名**：`deploy/tenants.hangzhou.json` → `deploy/tenants.test.json`（内容不变：fuduoduo/8103），头注释 + 租户 name 的「杭州」→「test」。
- **CI 引用**：`.gitea/workflows/deploy.yml` `for env in hangzhou` → `for env in test`（render 自动产 `compose-test.yml`）；`deploy/deploy-targets.json` name `杭州`→`test`、env `hangzhou`→`test`（host 47.118.16.235 不变）。
- **脚本注释**：`scripts/ci-fanout.sh:14`、`deploy/deploy-remote.sh:3,8` 注释 hangzhou/杭州→test（`deploy-remote.sh:12` 的 `compose-${ENV}.yml` 本就动态拼接，无需改）。
- **操作文档**：`docs/deploy/{README,sourceless-target-setup,cicd-gitea}.md` 的 hangzhou/杭州→test。**不改** `docs/superpowers/2026-06-25-*.md`（历史设计存档）。

### 原因
机器身份对齐命名、消除死文件误导（此前正是因 prod3.json 等死文件内容过时，叠加未先查 `.gitignore`，一度误判 `tenants.json` 为共用 tracked 文件、推断出「prod2/prod3 仓库分叉/定时炸弹」的乌龙）。全链路 env 动态拼接，改名后 test 机继续跑 fuduoduo，行为等价。

### 已知遗留（本次不修）
`docs/deploy/cicd-gitea.md` 的 ASCII 拓扑图与「3 台角色」节仍是 2026-06-25 主控迁移**前**的描述（把 prod2 当主控、说「触发改为手动 workflow_dispatch」等），与现状（push 自动触发、主控=prod3）不符，需后续单独更新。本次仅将其中的「杭州」机械替换为「test」。

### 验证
- `python3 -m json.tool deploy/tenants.test.json`：合法 JSON。
- `python3 scripts/render-multitenant-deploy.py deploy/tenants.test.json --skip-validation --compose-out /tmp/compose-test.yml --traefik-out /dev/null`：成功，含 `fuduoduo-api` + `neo4j:`。
- `grep hangzhou`（代码/配置层，排除 `docs/superpowers/`、`tests/`、`changelog/` 历史）：零残留。
- 引用一致：`deploy.yml` `for env in test` ↔ `deploy-targets.json` `env: test` ↔ `tenants.test.json`。

---

## 清理孤儿代码/资源 + 过时文档修正

### 概述
对仓库做了一次只读审计（4 个子代理分头扫），清理零引用的孤儿代码/脚本/静态资源/数据副本/未用依赖，
并修正 README 中认知颠倒与死引用的过时文档。所有删除项均经 grep 复核零引用 + import 冒烟 + 单测验证，
不影响运行时。

### 改动明细

#### 1. 孤儿代码（零 import）
- **删除**：`src/services/model_call_logger.py`（孤儿服务，零引用；⚠️ 同名 ORM `models/model_call_log.py` **保留**，仍被 `agent_migration.py`/`admin.py` 活跃使用）、`src/integrations/dingtalk/delivery_logger.py`。
- **原因**：定义后从未接入，仅构建产物 `SOURCES.txt` 自动登记。

#### 2. 钉钉旧版静态资源（已被统一模板取代）
- **删除**：`static/block_breakthrough.html`、`small_win.html`（`/quick` 路由实际渲染 `quick_trigger.html`，见 `quick_entry.py:223/236`）、`quick_entry_icon.png`（图标改名 `coach_mode.png`，`_upload_quick_entry_icon` 加载的是后者）。
- **保留**：`quick_trigger.html`、`cocah.html`、`cocah1.mp4`、`coach_mode.png`（均在用）。
- **原因**：删除前已核实渲染逻辑——`small_win_appreciation`/`sales_block_breakthrough` 两个 task_type 本身是活的，仅 H5 模板已合并为 `quick_trigger.html`，避免重蹈 `cocah.html` 500 覆辙。

#### 3. 一次性 SQL 补丁 + 孤儿 eval
- **删除**：`scripts/add_conversation_stage.sql`、`add_feedback_review_status.sql`、`add_agent_id_columns.sql`（无 Alembic 时代的手写 DDL，列已进模型 + Alembic migration）、`eval/workflow_test.jsonl`（零加载；⚠️ `smoke_test.jsonl` 仍被 `eval_runner_service` 加载，保留）。

#### 4. 未用依赖
- **删除**：`pyproject.toml` 的 `pydantic-settings`（全仓零 `BaseSettings` import）。

#### 5. 重复/孤儿数据
- **删除**：`data/Taishan/`（64 md，与运行时挂载卷 `data/taishan/Taishan/` md5 完全一致的孤儿副本，无 compose 引用）；工作区 `.DS_Store`（`.gitignore:50` 已有规则）。
- **保留**：`data/taishan/`、`data/taishankaifa2/`（运行时挂载卷，绝不可删）。
- **未清理**：`data/agents/*/`（目录名 UUID 与 DB agent id 格式不对应，语义待查；且 gitignored 不影响仓库，不清理比误删安全）。

#### 6. 根目录散落文件（git rm 确认）
- **删除**：`test.md`（空）、`tiyanbanshixian.md`（引用的代码路径全不存在）、`README_DEPLOY.md`（`package-deployment.sh` 动态生成）、`ontology-toolkit.zip`（4MB 第三方源码 zip）。
- **保留**：`changmodel.sh`（用户本地 Claude Code 工具，已 gitignore）；`milestone_plan.md`（归档/删需拍板）。

#### 7. README 过时文档修正
- **认知纠正**：41-63 行「RAG 知识库」节原把 pgvector chunk RAG 当主力、把生产实际在用的 Neo4j 说成「可选」——认知颠倒。改为 **Neo4j 本体引擎为生产默认**、`legacy_rag` 降为回退模式（正是 lessons #15 那类文档滞后坑）。
- **死引用**：项目结构图 `secrets/taishan.env.example`→`example.env`、删 `services/model_call_logger.py` 行、删 5 个全不存在的「Spec 文档」死表。

#### 8. docs 过时/重复/死链
- `docs/ontology-neo4j-ops.md`：上传类型补 `.doc`/`.xlsx`（同步新格式功能）。
- 删 `docs/multitenant-deployment.md`（与 `docs/deploy/multitenant-deployment.md` 字节重复）。
- 修 `docs/deploy/README.md:199` 死链（`deployment-roles.md` 在 `docs/` 根，相对链接改 `../`）。

### 验证
- 死代码残留 grep（文件/import 级，排除 changelog 历史）：**零残留**。
- `model_call_log` ORM 仍在（`agent_migration.py:56` 引用，未误删同名 ORM）。
- `python -c "from sales_agent.integrations.dingtalk import quick_entry; from sales_agent.services import agent_executor; from sales_agent.api import routes"`：**imports OK**。
- `pytest tests/unit/dingtalk/ tests/unit/coach/`：**161 passed**。

### 本次未处理（需另行拍板，非本批范围）
- 🚨 `infra/domain.key`、`domain.crt`、`gitea-admin-token.txt` 凭证明文入库（需 revoke + `git filter-repo` 清历史，高危须人工）。
- `milestone_plan.md` 归档 vs 删；`anniu.md`/`pcanniu.md` 归档 `docs/dingtalk/`（`anniu.md` 有代码注释引用需同步）；`deploy/tenant.env.example` vs `secrets/example.env` 双模板；`docker-compose.yml` 的 `taishan-agent` 单容器块；`bootstrap-new-master.sh` heredoc 与 `cicd-compose.yml` 配置漂移。

---

## 拍板清理第二批：domain 死证书 + taishan-agent 块 + milestone/anniu 归档 + bootstrap 去 TLS

### 概述
处理上一批「未处理待办」中用户拍板的项。**先 SSH 核实主控** `Ts-benji`（47.120.55.219）的 `sales-agent-registry`：ENV 仅 `htpasswd`、无 `REGISTRY_HTTP_TLS_*`、挂载仅 `htpasswd`——**主控 registry 也不用 TLS**。据此安全删除死证书并对齐 bootstrap。

### 改动明细

#### 1. 删除死 TLS 证书（主控+本机 registry 均已 HTTP+htpasswd，核实确认）
- **删除**：`infra/domain.key`、`domain.crt`、`registry-ca.crt`（git rm）。
- **核实依据**：SSH 主控 `docker inspect sales-agent-registry` → 无 TLS ENV/mount；本机 `cicd-compose.yml` 同 HTTP+htpasswd。
- **.gitignore**：新增 `infra/{domain.key,domain.crt,registry-ca.crt}` 防再入。
- **原因**：registry 走 HTTP+htpasswd，证书纯遗留；私钥入库是安全问题。

#### 2. bootstrap 对齐 cicd-compose（去 TLS）
- **改动**：`scripts/bootstrap-new-master.sh` 删除①openssl 证书生成段（ca.key/registry-ca.crt/domain.key/crt + SAN heredoc + CSR 签发）②registry heredoc 的 `REGISTRY_HTTP_TLS_*` ENV + domain.crt/key 挂载③`10b` 段 `cp registry-ca.crt → docker certs`。
- **原因**：bootstrap 生成的 registry 曾配 TLS，但主控/本机实际均 HTTP+htpasswd；对齐后新主控初始化不再生成无用证书、配置一致。保留「如需启用 TLS」注释说明。

#### 3. 删 taishan-agent 单容器块
- **改动**：`docker-compose.yml` 删 `taishan-agent` 服务块（`profiles: ["taishan"]`）+ 顶部 `--profile taishan` 注释示例；`docs/deployment-roles.md` 删「单容器模式」节；`README.md` Key 轮换示例 `--profile taishan` → `taishan-split`。
- **依据**：`deploy-image-retag.sh:12` `PROFILE="${PROFILE:-taishan-split}"`——prod2 默认 split，单容器块无实际部署使用。

#### 4. milestone_plan.md + anniu/pcanniu 归档
- **删除**：`milestone_plan.md`（git rm --cached；早期 M0-M7 规划已过期，README Spec 死表已清）。
- **归档**：`anniu.md`/`pcanniu.md` → `docs/dingtalk/`（git mv）；`quick_entry.py` 4 处 `anniu.md` 注释 → `docs/dingtalk/anniu.md`。

#### 5. 清 data/agents 孤儿目录
- **删除**：`data/agents/*/` 38 个空目录（rm -rf，gitignored；DB 实际仅 3 个 agent，UUID 目录名与 agent id 不对应，运行时按需重建）。

### 验证
- `bash -n scripts/bootstrap-new-master.sh`：语法 OK；TLS 配置/openssl 命令清零（仅留注释说明）。
- `python yaml.safe_load(docker-compose.yml)`：OK，services 仅 `postgres/neo4j/taishan-{api,stream,worker}`。
- `--profile taishan`（非 split）全仓残留：清零。
- `pytest tests/unit/dingtalk/`：**81 passed**。

### 仍未处理（需再拍板）
- **`deploy/tenant.env.example` 不是冗余**：被 `scripts/package-deployment.sh`（离线 release bundle）+ 多份离线部署文档实际使用，与 `secrets/example.env`（CI 在线部署）**用途不同**。上轮审计误判为「双模板重复」——删除会破坏离线打包流程，故保留。
- **registry-ca.crt 迁移流程遗留**：`migration-update-target.sh`、`.gitea/workflows/migrate-master.yml`、`docs/deploy/{cicd-migration,sourceless-target-setup,README}.md` 仍有 `scp/cat registry-ca.crt → docker certs` 的 client TLS 信任流程。registry 已改 HTTP 后失效，但属「已完成的一次性主控迁移」（2026-06-25），与 `migrate-master.yml` 归档是一揽子，本次不动。
- **git 历史仍含已删凭证**：`domain.key` 等已从工作区+索引删除，但 git 历史仍有。内网+registry 已不用，风险可控；彻底清除需 `git filter-repo` + force push（高危，待定）。

---

## CI/CD 同步运维脚本到无源码目标机（stop-tenant.sh / check-tenant.sh）

### 概述
`git push origin main` 触发的 CI 对 `image-deploy` 方式的无源码目标机（目前 test `47.118.16.235`）原本**只传 Docker 镜像、不传任何 .sh 脚本**，导致目标机上没有 `stop-tenant.sh`，想停 agent 只能手动 `docker stop sales-agent-<id>-api`。本次在 `ci-fanout.sh` 的 image-deploy 分支、`docker run` deploy 镜像之前，插入一步：用 `tar | ssh` 把两个**自包含**运维脚本同步到目标机 `${dir}/scripts/`，目标机上 `bash scripts/stop-tenant.sh` 即可停服务。CI runner 跑在主控 host mode 且已免密 ssh 到各目标机，同步零额外认证成本。

### 改动明细
- **改动对象**：`scripts/ci-fanout.sh`
- **类型**：功能增强（CI/CD 链路补齐）
- **影响范围**：仅 `method=image-deploy` 的远程目标机（test）。不影响 prod3（本机源码树自带脚本）、不影响有源码远程机（走 git sync 自带）。
- **改动明细**：image-deploy 分支在 `docker run` 之前新增 `tar -C scripts -cf - stop-tenant.sh check-tenant.sh | ssh ... "mkdir -p ${dir}/scripts && tar -xf - && chmod +x ..."`，失败用 `|| echo ⚠️` 兜底、不阻断后续部署（与该分支原有 `|| echo ⚠️` 风格一致）。
- **同步清单**：仅 `stop-tenant.sh` + `check-tenant.sh`（均自包含）。**不同步** `deploy-release.sh`（无源码机走 deploy 镜像、不走该流程）、`check-all-tenants.sh`（依赖目标机没有的 `deploy/tenants.json`）。
- **原因**：`stop-tenant.sh` 只用 `docker ps/stop/rm`，且目标机容器名为 `sales-agent-<id>-<role>`（render 脚本显式设 `container_name`），与脚本前缀假设完全匹配，落盘即可用。

### 实现要点（踩坑）
- `tar | ssh` 传输的 ssh **不能带 `-n`**：`-n` 把 ssh 的 stdin 重定向到 `/dev/null`，会丢弃管道里的 tar 流，远端 `tar -xf -` 报 "This does not look like a tar archive"。去掉 `-n` 后 stdin 由 tar 管道占用，也不会读走外层 `while read` 循环的 `/tmp/ci-targets.txt`（与其它分支用 `ssh -n` 的原因不同）。该分支是唯一不带 `-n` 的 ssh 调用，已在注释里标明。

### 验证
- `bash -n scripts/ci-fanout.sh`：语法 OK。
- 单独对 test 跑同步片段：test 机 `/root/code/sales-agent/scripts/` 出现 `stop-tenant.sh`(5293B)+`check-tenant.sh`(609B)，均带 `+x`。
- 功能预览：test 上 `bash scripts/stop-tenant.sh --tenant fuduoduo`（不带 `--yes`）正确列出 `sales-agent-fuduoduo-{api,stream,worker,frontend}` 四个容器，证明脚本在目标机可用。（注:非 tty 下 `read` 确认失败触发 `set -e` 以 exit 1 结束，属管道副作用，真实人工 tty 交互不受影响。）

### 教训关联
`ssh -n` 与管道传 stdin 互斥——见 lessons 新增条目。
