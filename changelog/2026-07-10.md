# 2026-07-10 升级日志

## 销售动作：钉钉卡片投递 + 按钮回调 + 调度器（Task 5）

- **改动对象**：`src/sales_agent/services/sales_actions/card_renderer.py`（新增）、`src/sales_agent/services/sales_actions/scheduler.py`（新增）、`src/sales_agent/integrations/dingtalk/sales_action_callbacks.py`（新增）、`src/sales_agent/services/sales_actions/repository.py`（扩展）、`src/sales_agent/roles/worker_runner.py`（接线）、`src/sales_agent/main.py`（注册路由）；测试 `tests/unit/sales_actions/test_card_renderer.py`、`tests/unit/sales_actions/test_scheduler.py`、`tests/integration/test_sales_action_scheduler.py`、`tests/integration/test_sales_action_callbacks.py`（新增）。
- **类型**：新功能（后台调度器 + 钉钉互动卡片投递 + 卡片按钮回调）+ 仓储扩展。
- **影响范围**：
  - 新增后台 worker loop（`sales_action_scheduler_loop`），由 `worker_runner.py` 在 `settings.sales_actions.enabled and scheduler_enabled` 时启动，`finally` 中取消。
  - 新增 `POST /integrations/dingtalk/sales-actions/callback` 路由（验签 + 内部契约），处理 complete/snooze/cancel 按钮并 `update_card` 重渲染。
  - 仓储 `claim_due_reminders` 现在一次 SKIP LOCKED 同时认领「到期 scheduled」与「failed + next_attempt_at 过期 + attempts<max_attempts」，修复 Task 3 遗留「一次失败即静默死亡」。
  - 在线请求路径零变更；调度器默认关闭（`sales_actions.enabled=False`）。
- **改动明细**：
  - **card_renderer**：纯函数渲染 due 提醒 / 早间 digest / 晚间 digest / 按钮回执；只引用回调所需字段（action_id/标题/客户/时间），不输出 `context_snapshot`/`source_event_id` 等敏感上下文；`build_digest_idempotency_key == "{kind}:{tenant}:{agent}:{user}:{date.isoformat()}"`。
  - **scheduler**：`run_sales_action_scheduler_once(session_factory, sender_factory, now)` 认领→渲染→`send_markdown_card`→`record_delivery_success/failure`→幂等创建 digest；`compute_sales_action_backoff_seconds` 委托仓储 `_bounded_backoff`（`min(300, 2**max(1,n))`）；digest 按 `settings.sales_actions` 本地时间窗口（09:00 / 18:30 Asia/Shanghai，2h grace）+ 幂等键去重创建；loop 管理发送器生命周期（构造一次、finally 关闭）。
  - **repository 扩展**：`claim_due_reminders(now, limit, max_attempts=5)` 用 `or_(scheduled-due, failed-retryable)` 单条 SKIP LOCKED；新增 `get_card_for_reminder`、`list_cards_for_scope`、`list_active_action_scopes`、`reminder_idempotency_key_exists`、`add_digest_reminder`。
  - **callbacks**：内部契约 `SalesActionCallbackRequest(action_id/tenant/agent/user/button/event_id/new_time?/card_instance_id?)` + `handle_sales_action_callback`（可注入 fake sender 单测）；仅在 `status∈{done,cancelled,snoozed}` 时重渲染，`not_found` 不写回执；钉钉原始载荷→内部契约映射留 `normalize_dingtalk_card_callback` TODO。
- **原因**：Task 5 of 销售动作计划——把 Task 1-4 的状态机接到钉钉卡片投递与用户按钮交互，补齐失败重试与每日 digest。
- **验证**：TDD RED（模块缺失）→ GREEN。`PYTHONPATH=src pytest -q tests/unit/sales_actions tests/integration/test_sales_action_scheduler.py tests/integration/test_sales_action_callbacks.py tests/integration/test_sales_action_repository.py` 全绿（90 项）；Task 4 graph 回归 `tests/unit/graph/test_online_graph.py tests/unit/dingtalk/test_online_flow_routing.py tests/integration/test_dingtalk_sales_actions.py` 全绿（126 项），无回退。测试用 FakeSender 捕获 send/update，零真实网络/LLM。

---


## 销售动作：运维 API + 只读运维页（Task 6）

- **改动对象**：`src/sales_agent/api/routes/sales_actions.py`（新增）、`src/sales_agent/services/sales_actions/repository.py`（新增 ops 读取方法）、`src/sales_agent/main.py`（注册路由）；前端 `console/src/api/salesActions.ts`、`console/src/pages/Agents/AgentSalesActionsPage.tsx`（新增）、`console/src/App.tsx`、`console/src/layout/AgentLayout.tsx`（接线）；测试 `tests/integration/test_sales_action_api.py`（新增）。
- **类型**：新功能（运维只读 API + 控制台只读页）。
- **影响范围**：
  - 新增 `GET/POST /agents/{agent_id}/sales-actions`、`GET /{action_id}`、`POST /{action_id}/complete|cancel|snooze`、`GET /reminders`、`GET /deliveries`（前缀 `/agents`，与本仓库既有约定一致；非计划文档的 `/api/v1/...`）。
  - 仓储新增 agent 维度只读方法（`list_actions_for_agent`/`get_action_detail`/`list_reminders_for_agent`/`list_deliveries_for_agent`），按 (tenant, agent) 聚合、user_id 作可选过滤；变更端点复用幂等状态机（与钉钉回调同路径，FOR UPDATE 锁定）。
  - 控制台新增「销售任务」只读页（Agent 子导航）：状态/类型/用户/计划时间过滤 + 详情抽屉（卡片 + 提醒/投递/事件），无编辑按钮。
- **改动明细**：
  - 路由用 `_load_agent`（含 `_enforce_tenant`）解析 tenant；变更端点从卡片反推 `SalesActionScope` 后调 `complete_action/cancel_action/snooze_action`。
  - 可选过滤参数用 `= None`（非 `Query(None)`），以便直接 async 调用处理函数做单测（`Query(None)` sentinel 不会被解包）。
- **原因**：Task 6 of 销售动作计划--给运维提供任务/提醒/投递/事件的可观测性与只读操作入口。
- **验证**：`PYTHONPATH=src pytest -q tests/integration/test_sales_action_api.py tests/unit/sales_actions tests/integration/test_sales_action_{repository,scheduler,callbacks}.py` 全绿（104 项）；`cd console && npm run build`（tsc+vite）通过、`npm test`（vitest）11 项全绿。无回退。

---

## 销售动作：评估数据集 + 发布门禁 + 运维手册（Task 7）

- **改动对象**：`eval/sales_actions/action_scenarios.jsonl`（新增，12 场景）、`eval/run_sales_action_eval.py`（新增）、`tests/unit/eval/test_sales_action_eval.py`（新增）、`tests/support/dingtalk_scenario.py`（ExpectedTurn 加销售动作期望字段）、`scripts/run_sales_action_gate.sh`（新增）、`docs/runbooks/sales-actions.md`（新增）、`README.md`（功能说明 + 更新日志索引）。
- **类型**：测试基建（确定性 fixture eval + 发布门禁）+ 文档。
- **影响范围**：
  - fixture eval（`run_fixture_eval`）确定性校验数据集内部一致性：clear_create/clarification 路由期望、无重复投递、无内存污染；`model`/`live` 模式为 CLI stub（不伪造指标，指向集成测试与 deepeval）。
  - 12 场景覆盖：显式创建/默认时间/模糊澄清/缺动作澄清/建议确认/到期投递/完成按钮/推迟按钮/取消按钮/早间 digest/晚间 digest/不入长期记忆。
  - 门禁 `scripts/run_sales_action_gate.sh`：unit + 4 集成 + fixture eval，`TEST_DATABASE_URL` 必须含 `test`。
  - `dingtalk_scenario.ExpectedTurn` 加 `sales_action_operation/status/creates_sales_action/duplicate_send` 期望字段。
- **原因**：Task 7 of 销售动作计划--给销售动作特性配确定性发布门禁与运维手册。
- **验证**：`TEST_DATABASE_URL=…test… bash scripts/run_sales_action_gate.sh` 全绿（unit 62 + 集成 42 + fixture thresholds_met=true，exit 0）。

---

## 销售动作：修复图路由未受 enabled 开关门控（最终评审 I-1）

- **改动对象**：`src/sales_agent/graph/online/state.py`（加 `sales_actions_enabled` 字段）、`src/sales_agent/services/online_conversation.py`（`build_online_turn_input`/`prepare_online_turn` 透传 `settings.sales_actions.enabled`）、`src/sales_agent/graph/online/nodes.py`（`normalize_turn_node` 两条 sales_action 分支加 `state.get("sales_actions_enabled")` 门控）；测试 `tests/unit/graph/test_online_graph.py`（路由测试置 flag + 新增 disabled 不路由测试）、`tests/integration/test_dingtalk_sales_actions.py`（autouse fixture 开启 flag）。
- **类型**：bug 修复（最终全分支评审发现）。
- **影响范围**：默认 `sales_actions.enabled=false` 时，Online Graph 此前仍会路由到 sales_action 节点 -> 创建卡片 + 回复「已创建提醒」，但调度器（已正确门控）永不触发 -> 机器人承诺永不兑现的提醒。修复后「关闭即不路由」与 runbook 一致。
- **改动明细**：复用 `long_term_memory_enabled` 模式；待澄清分支与显式命令分支均门控（关闭即彻底不路由，pending partial 变惰性）。
- **原因**：最终全分支评审跨任务+runbook 交叉发现，Task 4 评审只验了路由正确性、未验 feature-flag 门控。
- **验证**：`bash scripts/run_sales_action_gate.sh` exit 0（unit + 集成 + fixture）；新增 `test_sales_action_disabled_does_not_route`。

---

## LLM 调用参数配置化（temperature/max_tokens 收进 YAML）+ md_optimizer bug 修复

- **改动对象**：新增 `src/sales_agent/llm/call_params.py`（CallParams loader）、`config/llm_call_defaults.yaml`（24 个调用点默认值）、`tests/unit/llm/`（loader 单测）；改 `src/sales_agent/core/config.py`（Settings 加 `llm_call_defaults_path`）、`src/sales_agent/roles/stream_runner.py`（启动加载）、`tests/conftest.py`（测试 session 加载 fixture）；24 个 LLM 调用点（路由/生成/话题/风险/记忆/知识抽取/教练/场景/API/健康/媒体层）把硬编码 `temperature`/`max_tokens` 改为 `get_call_params("<call_site>")`；`services/md_optimizer.py`（修 `.chat()` bug）；`services/prompt_defaults.py` + `integrations/dingtalk/media_adapter.py`（6 个漏网 prompt 进 BUILTIN_PROMPTS）。
- **类型**：重构（配置解耦）+ bug 修复（md_optimizer latent crash）。
- **影响范围**：所有 LLM 调用点的参数来源。**值不变纯结构重构**——YAML 填入的就是原字面量，runtime 行为零变化（单测 `test_load_real_defaults_file_all_keys` 用 48 条断言核对全部 24 个 key 的值 = 原值）。**无 DB 变更、无 Alembic migration、无后台 API、无前端**。仅开发者改 YAML、git 管版本/回滚。
- **改动明细**：
  - **loader**：`call_params.py` 启动 `load_call_params()` 读 YAML + 校验（温度 ∈ [0,2]、max_tokens > 0、缺文件/缺字段/类型错 fail loud）+ 缓存进内存 dict；运行时 `get_call_params(call_site)` 零 IO 读取，未知 key 抛 `KeyError`。
  - **调用点接入**：22 个直调点 + 2 个间接 helper——`ontology/extractor.py` 的 `_generate_with_retry`、`coach_flows.py` 的 `_llm_generate` 各加 `call_site: str` 参数，多个业务点传不同 key（实体抽取 5000 vs 事实抽取 6000；教练小赢卡/破框拆解/破框出卡各异）。`response_format` 等 3 处（retrieval_service/ontology_graph/daily_evaluator）保留代码不进 YAML（非目标：结构性参数留代码）。`health_ping` 原隐式温度（实例默认 0.3）改显式 0.3，消除「实例默认遮蔽」。`media_adapter` 两点走原生 SDK `client.chat.completions.create()`，读 `get_call_params` 后传给 create。
  - **md_optimizer bug**：`services/md_optimizer.py:146` 调 `self.chat_model.chat(...)`，但 `ChatModel` 只有 async `generate`/`stream_generate`（运行时 `AttributeError`，知识库 ingest 链路 latent crash）→ 改 `await self.chat_model.generate(...)`；同步删除 response 对象解析（`.content`/`.choices[0].message.content`），因 `generate()` 返回纯 str（只改方法名仍会崩）。所在函数 `optimize` 本已 async，无需传播。
  - **6 个漏网 prompt**：`MEMORY_EXTRACTOR_PROMPT`/`TOPIC_RESTORE_RESOLVER_PROMPT`/`SCENARIO_MATCHER_PROMPT` + media_adapter 3 个内联 prompt（先抽成模块常量）注册进 `BUILTIN_PROMPTS`，补 prompt 解耦漏洞（获得 DB 覆盖路径）。核实更正：原以为「重复的 `_ENTITY_EXTRACTION_PROMPT`」漏网，实为两个不同 prompt 且均已注册，非漏网。
- **原因**：① 参数硬编码散落 24 处、改一个值要改 py + 发版，无法集中管理；② md_optimizer 是知识库 ingest 链路上的 latent crash。用户最初设想「prompt 在 py 里迁 YAML + DB 快照」，经核实纠正：prompt 主要在 DB（py 仅兜底），迁 YAML 反而降级；版本/回滚/防误删 git 全包，DB 快照与 git 重复。最终只做参数 YAML 化（不做 DB/不做后台 API）。
- **验证**：全量 unit **1214 passed / 7 failed，7 个均 pre-existing**（extractor 去重测试设计 + ingestion FakeExtractor 签名漂移 + optimization/runtime asyncpg「different event loop」，经 `git stash` 与 merge-base `6a8f0ee` temp worktree 复跑确认，分支零新增失败）。最终全分支 code review：**merge-ready**（无 Critical/Important；Minor 均 accept-and-track：test_get_before_load_raises 改全局无清理、stale-value 注释、memory/topic_restore prompt 含未转义 JSON 大括号——后者仅在未来改走 resolve_prompt 时才会静默 mangle，当前直调无影响）。值不变由 `test_load_real_defaults_file_all_keys`（48 断言）+ 逐 task 两阶段 review 核实。spec/plan：`docs/superpowers/specs/2026-07-10-llm-call-params-config-design.md`、`docs/superpowers/plans/2026-07-10-llm-call-params-config.md`。

## DeepEval HTML 报告渲染 markdown（修复 agent 回答显示成源码）

- **改动对象**：`eval/deepeval_html_report.py`、新增 `eval/vendor/marked.min.js`（marked v12.0.2，MIT）。
- **类型**：bug 修复（评估工具展示层）。
- **影响范围**：仅 `eval/deepeval_run.sh` 生成的 HTML 报告（`eval/results/deepeval/*.html`）。**不改动在线 Graph 请求路径**、不影响钉钉/HTTP 生产链路、不影响 MD/CSV 报告、无 DB 变更、无配置变更。报告保持自包含（浏览器离线打开即可渲染）。
- **改动明细**：
  - **根因**：`deepeval_html_report.py` 的 HTML 报告是客户端 JS 渲染（数据 `json.dumps` 内嵌、`innerHTML` 拼装），「完整回答」与「参考答案」字段只做了 `.replace(/</g,'&lt;').replace(/>/g,'&gt;')` 尖括号转义，**没有 markdown→html 转换**，导致 `##`/`-`/`**`/`|...|` 全部原样显示成源码。整个报告所有文本字段都如此，回答字段因内容长、markdown 密集而最显眼。
  - **修复①内嵌 marked**：新增 `eval/vendor/marked.min.js`（marked v12.0.2 minified，35KB，UMD）；`_load_marked_js()` 在报告生成时读取并内嵌进 `<script>` 块——源码文件保持干净，报告仍自包含可离线打开。文件缺失时回退纯文本转义，不阻断报告生成。
  - **修复②mdToHtml 渲染**：新增 JS `mdToHtml(text, maxLen)`——**先转义 `&` 和 `<` 防 XSS（刻意保留 `>` 以识别 GFM blockquote），再 `marked.parse(s, {gfm:true, breaks:true})`**。「完整回答」/「参考答案」两字段改调它（含 2000 字截断保持原行为）。配套新增 `.v.md` CSS：标题/列表/表格/代码块/blockquote/链接样式。
  - **不渲染「指标理由」**：`metric_reasons` 是 LLM-as-judge 的自然语言、非 markdown，保留原 `white-space:pre-wrap` 纯文本展示。
- **原因**：用户报告 HTML 报告里 agent 回答的 markdown 不渲染。修复后表格/标题/列表/代码块正常展示，且防住 `<script>` 等 inline HTML 注入。
- **验证**：① `python -m py_compile eval/deepeval_html_report.py` 通过；② 构造含标题/加粗/列表/表格/代码块/blockquote + `<script>alert(1)</script>` 的测试数据生成 HTML，静态确认 marked 库内嵌 + `mdToHtml` 定义 + 两字段调用 mdToHtml；③ **node 实跑 marked + mdToHtml 等价逻辑**：`##`→`<h2>`、`**x**`→`<strong>`、列表→`<ul><li>`、**表格→`<table><thead><th>+<tbody><td>`**、代码块→`<pre><code class="language-python">`、blockquote→`<blockquote>` 全部命中；XSS payload 输出 `<p>&lt;script&gt;alert(1)&lt;/script&gt;</p>`（无可执行 `<script>` 标签）。markdown 渲染仅在浏览器端 JS 执行，Python 端零新增依赖（venv 里的 `markdown-it-py` 是 `rich` 传递依赖，本次未引入）。

## Scenario Coach 钉钉渲染缺分组标题「二、再应对」（修复 loader 丢弃空正文父标题）

- **改动对象**：`src/sales_agent/scenarios/loader.py`、`src/sales_agent/services/response_formatter.py`、`tests/unit/test_scenario_loader.py`、`tests/unit/test_response_formatter.py`。
- **类型**：bug 修复（scenario_coach 渲染的数据层 + 输出层）。
- **影响范围**：仅 scenario_coach 命中时的预设答案渲染（钉钉 stream 卡片 + HTTP `format_text_output`）。不影响路由、RAG、生成、DB。所有「需判断型」场景（Q01/Q02/Q03/Q04…，6 个场景里的大部分）此前都缺「二、再应对」这一行分组标题——正文内容本身不丢，只是缺这行统领标题。
- **改动明细**：
  - **根因**：手册里 `**二、再应对：判断完，对应着打**` 这类分组父标题下面直接接 `####` 子标题、自身没有独立正文。`loader.py` 的 `_split_sections` 把它解析成一个 `content=""` 的 section，随后被末尾的 `[s for s in sections if s.content]` 一刀切丢弃，分组标题永不进入 `answer_dict`，钉钉怎么渲染都出不来这行。`format_text_output` 的 `if title and content` 是第二层同样的空正文过滤。
  - **修复①loader**：`_split_sections` 末尾改成只按 title 过滤（`if s.title`），保留分组父标题（content 可空）。
  - **修复②formatter**：`format_text_output` 对空 content 的 section，作为纯标题行输出（`lines.append(title)`），不再因 content 为空而跳过。
  - **测试**：新增 `test_group_header_without_body_preserved`（loader）、`test_group_header_without_content`（formatter）两个先红后绿回归；放宽 `test_question_fields_populated` 的「每个 section 必有 content」契约（分组标题可无正文）。
- **原因**：用户测试 scenario_coach 命中后，钉钉端渲染缺「二、再应对：判断完，对应着打」这行分组标题（其余内容完整）。一度怀疑流式截断——实为 loader 数据层 bug，命中走的是非流式 `format_text_output`。
- **验证**：① scenario 全套单测 43 passed（`test_scenario_loader/matcher/config/citation` + `graph/test_scenario_coach_node/coach_graph` + `test_response_formatter`）；② 端到端 `parse_scenario_md` + `format_text_output` 跑 Q04，输出含「二、再应对：判断完，对应着打」且四个 `####` 子节正文齐全。
## 联网搜索：部分命中实体缺口补全

- **改动对象**：ontology 检索路径（`graph/chat/nodes/retrieval.py`）、`graph/retrieval/web_fallback.py`、`graph/retrieval/gap_fill.py`（新增）、`prompts/web_analysis_prompt.py`、`core/config.py`。
- **类型**：功能增强（检索层）。
- **影响范围**：hybrid 部署（`KNOWLEDGE_ENGINE=ontology_neo4j` + `HYBRID_RETRIEVAL=true`，生产默认）下，当 query 提到的品牌/产品实体只有部分在知识图谱命中时（如「全品C 和 X品牌 区别」，全品C 在 KB、X品牌 不在）。无 DB 变更、无配置破坏性变更（新字段 `max_gap_entities` 默认 2）。旧 `_retrieve_via_rag` 路径与 `policy=web` 整句域外主路径不动。
- **改动明细**：
  - 新增 `graph/retrieval/gap_fill.py`：纯函数 `is_entity_like`（剔除「区别/对比/怎么样」等非实体词与过短词）/ `is_covered`（双向大小写不敏感子串匹配，对齐 Cypher CONTAINS）/ `compute_missing`（search_terms 减命中实体名，保序去重、`max_n` 封顶）。
  - `_retrieve_via_ontology` 在 `compact_evidence` 之后做缺口检测：对未命中实体定向调 `web_fallback_and_analyze`（查询词 `"{实体} 产品 功能 介绍"`，`context_message` 保留原句意图），web 结果的 context 块与 sources **追加合并**到 KB 结果（取代原「全空才触发 + early-return 整体替换」）。原「全空」case 自然退化为「所有实体 missing -> web 填全部」。
  - `web_fallback_and_analyze` 新增可选 `search_query`（传给 Bocha，缺省退回 message）/ `context_message`（喂分析 LLM 的原句问题，缺省退回 message）；旧 3 个调用点零改动。`web_analysis_prompt` 加 `{user_question}` 段让分析看到用户原始意图。
  - `WebSearchConfig` 新增 `max_gap_entities: int = 2`，env `BOCHA_MAX_GAP_ENTITIES` 可覆盖。
  - web 失败/无结果/未配 key 时静默回退纯 KB（`web_fallback_and_analyze` 返回 None），不阻断主流程。
- **原因**：用户问「全品C 和某品牌 区别」时，全品C 在 KB、某品牌 不在，原管线因 `sources>0`（全品C 在）直接放行、只拿全品C 素材硬答；现对缺失实体走 web 补资料，引用区按来源标「知识库 / 网络搜索」。引用区分来源复用既有 `source_type`（ontology/web），生成侧零改动。
- **验证**：`gap_fill` 7 单测 + `web_search_config` 2 单测 + `web_fallback` 5 单测 + `retrieval_web_fallback` 6 单测（含部分命中合并/全命中不触发/封顶）全过；`test_retrieval_node` 等回归 25 项全过。生产 stream 端到端验证待部署。

## 钉钉卡片反馈采集 M1（点赞/点踩/问题反馈多选 capture-first）

- **改动对象**：`src/sales_agent/integrations/dingtalk/models.py`、`src/sales_agent/integrations/dingtalk/card_callback_handler.py`（新增）、`src/sales_agent/integrations/dingtalk/stream_client.py`、`src/sales_agent/models/__init__.py`、`src/sales_agent/migrations/versions/0018_card_callback_events.py`（新增）、`tests/integration/test_card_callback_capture.py`（新增）。
- **类型**：功能新增（钉钉 Stream 接收侧 + DB 表）。
- **影响范围**：所有走 Stream 的租户。新增表 `dingtalk_card_callback_events`（migration 0018）。仅在既有 ChatbotMessage handler 之外**并列新增**一个卡片回调 handler 注册，不改动聊天/流式卡片主路径。无配置破坏性变更。
- **改动明细**：
  - **缺口**：卡片发送用 `callbackType=STREAM`，但 `stream_client` 只注册了 `ChatbotMessage.TOPIC`，未注册卡片回调 topic → 用户点赞/点踩/多选点了收不到、被丢弃。
  - **接收接线**：`start_dingtalk_stream_worker` 在聊天 handler 后追加 `client.register_callback_handler(CallbackHandler.TOPIC_CARD_CALLBACK, SalesAgentCardCallbackHandler(runtime.tenant_id))`。**无条件注册**——不发卡片的租户永不收到该 topic，零副作用。
  - **采集**：`SalesAgentCardCallbackHandler.process` 用 `CardCallbackMessage.from_dict` 解析，纯函数 `capture_card_callback(db,msg,tenant_id,raw)` 把 `user_id / card_instance_id(outTrackId) / corp_id / space_id / content / extension / 完整 raw` 落表；`content_json/extension_json/raw_json` 均 `ensure_ascii=False`。
  - **全租户优雅降级（约束①）**：`process` 全程 try/except——解析失败返回 `(STATUS_OK,"parse_error")` 不落库；DB 故障 `logger.exception` 后仍 `(STATUS_OK,"OK")`；**任何异常都不外抛到 Stream 主循环**，恒 ACK。
  - **归因免映射表**：回调无钉钉会话 id，但带 `user_id`；`conversation_mapper.generate_conversation_id(tenant_id,user_id)="dingtalk_single:{tenant}:{user}"` 确定性可重算（M2 用），故无需 out_track_id→conversation 映射表。
  - **表结构**：`dingtalk_card_callback_events`(id, tenant_id, dingtalk_user_id, card_instance_id, corp_id, space_id, content_json, extension_json, raw_json, processed, created_at, updated_at) + 索引 `(tenant_id,created_at)`、`(card_instance_id)`。`processed` 供 M2 回放标记。
  - **迁移 id 两个坑（验证抓到）**：① 初版 revision id `0017_dingtalk_card_callback_events`（34 字符）超 `alembic_version.version_num` 的 `varchar(32)`，upgrade 写版本号时崩——若上线，stream 容器启动跑 alembic 直接挂；缩短命名。② 合入时并发分支已带入 `0017_pursuit_loop_schema`（revision `0017`，同样 down_revision `0016`），两者并列会造成 alembic **双 head**、`upgrade head` 报多 head 错；故把本迁移线性化到其后：revision `0018_card_callback_events`、down_revision `0017`。
- **原因**：用户要求采集卡片点赞/点踩及点踩后的问题反馈多选。因 `content` 结构由钉钉后台模板定义、代码不可知，采 capture-first：M1 先注册+原样落库真实载荷，M2 再基于真样本定稿「raw → `feedbacks`」映射（点踩多选 → labels/categories）。
- **验证**：① 4 单测全过（`test_card_callback_capture`：真实 DB 落库+中文不转义、畸形 content 不抛恒 STATUS_OK 不落库、非 dict data 不抛、DB 故障仍 STATUS_OK）；② migration 在一次性 scratch DB 隔离验证 up（建表+2 索引+版本号合规）/down（删表）/再 up 幂等全通过；合并后确认 alembic 单一 head（0016→0017_pursuit_loop→0018_card_callback 线性）；③ import smoke 确认表进 `Base.metadata`、`stream_client` 正常 import。生产 stream 端到端验证（真实点击）待部署。
- **后续（M2，gated on 真样本）**：raw → `Feedback` 映射、幂等（append vs upsert）、多选落 labels/categories、会话不存在兜底。

## 三台部署验证 + 修复 alembic 双 head 与 customer_scope 幽灵漂移

- **改动对象**：`src/sales_agent/migrations/versions/20260710_596fe43c3337_prompt_yaml_cleanup.py`（改 down_revision）、`src/sales_agent/migrations/versions/0019_backfill_customer_scope.py`（新增 backfill migration）。
- **类型**：bug 修复（migration 链 + schema 幽灵漂移）+ 部署运维。
- **背景**：`git push origin main`（79 提交积压，含 prompt YAML 化重构）触发 deploy.yml，扇出 prod2/prod3/test 三台。首轮 deploy-fanout 失败，逐台核对发现两个独立问题。
- **问题①：alembic 双 head 致 upgrade 拒执行**
  - `596fe43c3337`(prompt_yaml_cleanup) 与 `0018_card_callback_events` 都以 `0017` 为 down_revision，形成双 head，`alembic upgrade head` 报 Multiple head revisions、整体拒执行。
  - 后果：prod3(taishanyanshi)/test(fuduoduo) 的 DB 卡在 `0015`；prod2 因原已在 `0018` 不受影响。
  - 修复：线性化 `596fe43c3337` 的 down_revision 到 `0018_card_callback_events`（两迁移内容独立、596 未在任一 DB 应用，改链安全）。真 alembic 校验单 head。commit `c1d298f`。
- **问题②：stamp-head 兜底跳过 add_column 造成幽灵漂移**
  - 双 head 修复后重部署，init_db 的 `create_all` 先于 `alembic upgrade` 抢建 0016/0017 新表 → upgrade 撞 DuplicateTableError → `_run_auto_migrations` 走 `stamp head` 兜底 → 跳过 0017 里 `agent_memories` 老表的 `add_column(customer_scope)`。alembic_version 标到 head 却缺列（schema 校验报幽灵漂移）。
  - 修复：仿 `0012_backfill_skipped_columns`，新增 `0019_backfill_customer_scope`（链在 596 后），用 `ADD COLUMN/CREATE INDEX IF NOT EXISTS` 幂等补齐。prod2(已有列)为 no-op，prod3/test 真正补上。commit `faa7723`。
- **影响范围**：仅 migration 链与 schema 修复，无生产代码行为变更。deploy-remote.sh 的 schema 一致性校验（`check_schema_consistency.py`）是本次能抓到漂移的关键防线。
- **验证（走生产入口 stream 日志）**：终轮部署三台镜像均 `faa7723`；schema 校验 5 租户全 ✅ 通过；stream 连钉钉——taishan/taishankaifa2/taishanyanshi/fuduoduo 4 租户 Restarts=0、已连 wss 网关、0 鉴权错误。
- **遗留（既有问题，非本次引入）**：prod3 **songbai** 租户 stream 持续 401 鉴权失败——`secrets/songbai.env` 凭证 7-02 后未变动（8 天），容器一启动即 401，与本次代码/部署无关；属 songbai 钉钉应用侧凭证失效（app key/secret 无效或未开通 Stream 权限），需租户在钉钉开放平台重配。schema/镜像本身正常。
