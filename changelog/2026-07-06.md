# 2026-07-06 升级日志

| 改动对象 | 类型 | 影响范围 | 改动明细 | 原因 |
|---------|------|---------|---------|------|
| `graph/nodes/retrieval.py` | 重构 | 所有 ChatPipeline 请求 | 添加 `_get_ontology_subgraph()` + `@lru_cache(maxsize=1)`，`_retrieve_via_ontology` 改为调用缓存子图而不是每次编译 | 消除 Ontology 子图重复编译开销（每个请求编译一次 → 进程生命周期内编译一次） |
| `graph/registry.py` | 新增 | Graph Debug / 所有 graph 消费者 | 创建统一 Graph Registry，仅注册 `online`、`guided-flow`、`ontology-retrieval` | 集中管理可构建图，替代 `chat_graph.py` 中的 `DEBUG_GRAPH_REGISTRY` |
| `graph/chat_graph.py` | 删除 | 注册表和 coach 图引用 | 移除了 `DEBUG_GRAPH_REGISTRY` 字典、三个 coach 图 import | 注册表已移至 `registry.py`；coach 图已被统一工作流取代 |
| `graph/__init__.py` | 修改 | 包导出 | 移除 `build_quick_session_graph`/`build_daily_eval_graph` 导出，添加 `GRAPH_REGISTRY` | 清理已退役的 coach 图 |
| `api/routes/graph_debug.py` | 修改 | Graph Debug 端点 | `DEBUG_GRAPH_REGISTRY` → `GRAPH_REGISTRY` | 适配新注册表路径 |
| `coach/quick_session.py` | 删除 | 数据层 | 整个文件删除（DB 会话服务） | 全部入口已转向 Guided Flow handlers（Task 3 已提取），无调用者 |
| `graph/coach/quick_session_graph.py` | 删除 | 图定义 | 整个文件删除（未使用的 stub 图） | 被 Unified Guided Flows 替代 |
| `graph/coach/daily_eval_graph.py` | 删除 | 图定义 | 整个文件删除（未使用的 stub 图） | 被 Unified Guided Flows 替代 |
| `models/quick_session.py` | 无变化 | — | 保留表和模型 | 历史和查询需要 |
